using System;

namespace Common
{
    public enum Mode
    {
        Default,
        Selected,
        Hover,
    }
    public enum CellColor
    {
        White,
        Red,
        Blue,
        Green,
        Yellow,
    }
}
